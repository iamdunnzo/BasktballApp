<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Court Details</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            background-image: url("https://cdn.shopify.com/s/files/1/0565/3381/1371/products/goalsetter-hd-breakaway-rim_basketballrim___1_1024x.png?v=1628186395");
            background-size: contain;
        }

        .navbar {
            background-color: #333;
            color: #fff;
            padding: 10px;
        }

        .container {
            background-color: rgba(255, 255, 255, 0.8);
            padding: 20px;
            text-align: center;
        }

        h1, h3, h4, h5 {
            margin: 0;
        }

        img {
            height: 200px;
            margin-top: 10px;
        }

        .players-container {
            margin-top: 30px;
        }

        .players-container h3 {
            margin-top: 0;
        }

        form {
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <div class="navbar">
        <h1>Basketball Courts</h1>
    </div>

    <div class="container">
        <h1>Court Details</h1>

        <h3>{{foundCourt.name}}</h3>
        <h4>Level: {{foundCourt.level}}</h4>
        <h5>Location: {{foundCourt.location}}</h5>

        <img src="{{foundCourt.image}}" alt="Court Image">

        <div class="players-container">
            <h3>Players</h3>

            {{#each players}}
                <p>{{this.name}}</p>
            {{/each}}
        </div>

        <br>

        <form action="/courts/add/{{foundCourt._id}}" method="POST">
            <button>Add this Court to My Page</button>
        </form>

        <br>

        <a href="/courts/{{foundCourt._id}}/edit"><button>Edit This Court</button></a>

        <br>

        <form action="/courts/delete/{{foundCourt._id}}" method="POST">
            <button>Delete This Court</button>
        </form>
    </div>
</body>
</html>
