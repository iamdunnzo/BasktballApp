<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit This Court</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            background-image: url("https://upload.wikimedia.org/wikipedia/commons/thumb/6/64/Edit_icon_%28the_Noun_Project_30184%29.svg/1024px-Edit_icon_%28the_Noun_Project_30184%29.svg.png");
            background-size: 65%;
            background-repeat: no-repeat;
            background-position: center center;
            text-align: center;
        }

        h1 {
            margin-top: 50px;
            color: red;
        }

        form {
            margin-top: 50px;
            display: inline-block;
            background-color: rgba(255, 255, 255, 0.7);
            padding: 20px;
            border-radius: 10px;
        }

        form div {
            margin-bottom: 10px;
        }

        form button {
            padding: 10px 20px;
            background-color: red;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

        form button:hover {
            background-color: #45a049;
        }
    </style>
</head>
<body>
    <h1>Edit This Court</h1>

    <form action="/courts/{{theCourt._id}}/update" method="POST">
        <div>
            Name:
            <input type="text" name="courtName" value="{{theCourt.name}}">
        </div>

        <div>
            Level:
            <input type="text" name="courtLevel" value="{{theCourt.level}}">
        </div>

        <div>
            Location:
            <input type="text" name="courtLocation" value="{{theCourt.location}}">
        </div>

        <div>
            Image:
            <input type="text" name="courtImage" value="{{theCourt.image}}">
        </div>

        <button>Save</button>
    </form>
</body>
</html>




