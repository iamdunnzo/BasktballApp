<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit This Basketball Player</title>
   <style>
        body {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            background-image: url("https://upload.wikimedia.org/wikipedia/commons/thumb/6/64/Edit_icon_%28the_Noun_Project_30184%29.svg/1024px-Edit_icon_%28the_Noun_Project_30184%29.svg.png");
            background-size: 65%;
            background-repeat: no-repeat;
            background-position: center center;
            text-align: center;
        }

        h1 {
            margin-top: 50px;
            color: red;
        }

        form {
            margin-top: 50px;
            display: inline-block;
            background-color: rgba(255, 255, 255, 0.7);
            padding: 20px;
            border-radius: 10px;
        }

        form div {
            margin-bottom: 10px;
        }

        form button {
            padding: 10px 20px;
            background-color: red;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

        form button:hover {
            background-color: #45a049;
        }
    </style>

<h1>Edit This Basketball Player</h1>

<form action="/players/{{thePlayer._id}}/update" method="POST">
  <div>
    Name:
    <input type="text" name="playerName" value="{{thePlayer.name}}">
  </div>

  <div>
    Position:
    <input type="text" name="playerPosition" value="{{thePlayer.position}}">
  </div>

  <div>
    Team:
    <input type="text" name="playerTeam" value="{{thePlayer.team}}">
  </div>

  <div>
    Height:
    <input type="number" name="playerHeight" value="{{thePlayer.height}}">
  </div>

  <div>
    Weight:
    <input type="number" name="playerWeight" value="{{thePlayer.weight}}">
  </div>

  <div>
    Age:
    <input type="number" name="playerAge" value="{{thePlayer.age}}">
  </div>

  <button>Save</button>
</form>
