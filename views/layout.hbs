<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GotNext</title>
    <style>
        /* Reset default margin and padding for all elements */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        /* Set a background color and font for the body */
        body {
            background-color: #f5f5f5;
            font-family: Arial, sans-serif;
        }

        /* Style the navigation bar */
        nav {
            background-color: #333;
            padding: 10px;
            text-align: center; /* Center the navigation links */
        }

        /* Style the navigation links */
        nav a {
            color: #fff;
            text-decoration: none;
            padding: 8px 12px;
            margin-right: 10px;
        }

        /* Style the navigation links on hover */
        nav a:hover {
            background-color: #555;
        }

        /* Style the logout button */
        nav form {
            display: inline;
            margin-right: 10px;
        }

        /* Style the logout button */
        nav form button {
            background-color: #c0392b;
            color: #fff;
            border: none;
            padding: 8px 12px;
            cursor: pointer;
        }

        /* Style the page content */
        .container {
            margin: 20px;
        }

        /* Add animations to the page content */
        @keyframes fadeIn {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }

        .container {
            animation: fadeIn 1s;
        }
    </style>
</head>
<body>
    <nav>
        <a href="/mypage">My Profile</a>
        <a href="/courts">See Courts</a>
        <a href="/courts/new">Add a Court</a>
        <a href="/players/new">Add New Player</a>
        <a href="/players">See All Players</a>
        <a href="/signup">Signup</a>

        {{#if theUserObject}}
        <form style="display:inline" action="/logout" method="POST"><button>Logout</button></form>
        {{else}}
        <a href="/login">Login</a>
        {{/if}}
    </nav>
    <div class="container">
        {{{body}}}
    </div>
</body>
</html>
